"use strict";

module.exports = {
  up: (queryInterface, Sequelize) => {
    throw Error("migrate task up failed");
  },

  down: (queryInterface, Sequelize) => {
    throw Error("migrate task down failed");
  }
};
